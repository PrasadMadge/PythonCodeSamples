# pip install command to install any package
# pip acts like a npm tool to install any package
# modules are just py script that you call in other py scripts
# seems like .jar file
# check module package to see the example